layers:
  0:
    d:
      data:
      - left_shift + left_command + d
      - m
      type: multi
    i:
      data: left_shift + 8
      type: single
    left_command:
      data: '1'
      type: layer|MO
    m:
      data: left_control + left_alt + h
      type: single
    tab:
      data: '2'
      type: layer|MT
  1:
    j,k:
      data: left_alt + delete_or_backspace
      type: single
  2:
    comma:
      app:
      - com.brave.browser
      - CLion
      app_mode: inclusive
      data: '2'
      type: single
    f:
      data:
      - t
      - e
      - s
      - t
      - return_or_enter
      type: multi
    m:
      data: open Safari
      type: shell
